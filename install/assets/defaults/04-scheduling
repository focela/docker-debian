#!/command/with-contenv bash
#-----------------------------------------------------------------------------
# Container Scheduling Configuration
#
# Purpose: Define default environment variables for container scheduling and cron jobs
# Context: Loaded by with-contenv wrapper during container initialization
# Note: These defaults configure logging for scheduled tasks and cron operations
#-----------------------------------------------------------------------------



#-----------------------------------------------------------------------------
# SCHEDULING LOGGING CONFIGURATION
#-----------------------------------------------------------------------------
# Logging settings for scheduled tasks and cron operations
SCHEDULING_LOG_TYPE=${SCHEDULING_LOG_TYPE:-"FILE"}
SCHEDULING_LOG_LOCATION=${SCHEDULING_LOG_LOCATION:-"/var/log/cron/"}
SCHEDULING_LOG_LEVEL=${SCHEDULING_LOG_LEVEL:-"8"}
