#!/command/with-contenv bash
#-----------------------------------------------------------------------------
# Container Messaging Configuration
#
# Purpose: Define default environment variables for container messaging and SMTP settings
# Context: Loaded by with-contenv wrapper during container initialization
# Note: These defaults configure SMTP client for email notifications and messaging
#-----------------------------------------------------------------------------



#-----------------------------------------------------------------------------
# SMTP CONFIGURATION
#-----------------------------------------------------------------------------
# SMTP server connection settings
SMTP_HOST=${SMTP_HOST:-"postfix-relay"}
SMTP_PORT=${SMTP_PORT:-"25"}
SMTP_DOMAIN=${SMTP_DOMAIN:-"docker"}
SMTP_MAILDOMAIN=${SMTP_MAILDOMAIN:-"local"}

# SMTP authentication and security settings
SMTP_AUTO_FROM=${SMTP_AUTO_FROM:-"FALSE"}
SMTP_STARTTLS=${SMTP_STARTTLS:-"FALSE"}
SMTP_TLS=${SMTP_TLS:-"FALSE"}
SMTP_TLSCERTCHECK=${SMTP_TLSCERTCHECK:-"FALSE"}
